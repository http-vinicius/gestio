'use client';

import TemplateLogin from '@/templates/template-login';

export default function Login({ children }: { children: React.ReactNode }) {
  return <TemplateLogin>{children}</TemplateLogin>;
}
